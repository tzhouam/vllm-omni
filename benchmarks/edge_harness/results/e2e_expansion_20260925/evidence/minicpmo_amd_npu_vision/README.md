# MiniCPM-o 4.5 vision-stage AMD NPU feasibility on HX370

**Disposition (2026-09-25): component C placement with failed held-out numerics; the PC AMD NPU MiniCPM-o cell remains NOT E2E.** The real checkpoint at revision `503e754207c94da6bb26850b4469f367c9ea3582` supplied a BF16 SigLIP vision transformer. The retained [red-square image](../../../e2e_expansion_20260923/evidence/minicpmo_wsl_image/red_square.png) was normalized to the model processor's 448-pixel, 32×32-patch bucket. The [export script](../../../../experiments/probe_minicpmo_vision_npu.py) pins the config, source and weight-shard hashes. It isolates a vision encoder block after patch/position embedding and before post-layernorm, so image preprocessing, resampling, the thinker, speech head and vocoder are outside the measured graph.

The four-layer [FP32 export](export_report.json) used a fixed `[1,1024,1152]` embedding and matched source FP32 Torch at **1.23e-6 relative L2** on ONNX Runtime CPU. Converting this BF16 checkpoint stage to FP32 already changed its four-layer output by **1.079% relative L2** against BF16 Torch. The 243,863,528-byte ONNX artifact remains outside Git at `/home/zhout/project/edge_infer/models/minicpmo_vision_prefix4_448_20260925.onnx`, SHA-256 `bd3e7802a78a930c5cd3e8e1a9888ab73e3fa865d6f831e492e169cdd24540be`. The exact generated input and reference are retained locally in `fixture.npz`, SHA-256 `9e58e815ccf46039b28d70f1b952f79ccc27b97a57ab44eaed50bd5218f6bcd2`; rerunning the export regenerates them from pinned source files.

The [native Windows quantization/probe script](../../../../experiments/probe_minicpmo_vision_npu_candidate.py) used one image activation for A16W8 per-channel MatMul/Gemm calibration. The [first four-layer candidate](full4_quantized_probe.json) placed **20 VitisAI node events per call** (400 over 20 calls) but missed the provisional 1% candidate-versus-FP32 numerical gate: **1.048%** on both ORT CPU and NPU. Its NPU output was within **0.0564%** of the same QDQ graph on CPU. That is an artifact-specific numerical failure, not a claim that the model or NPU cannot execute vision.

The [selective candidate](first3_quantized_probe.json) left the fourth layer's eight MatMul operations FP32 on CPU. It placed **15 VitisAI node events per call** (300 over 20) and passed the same fixed-input provisional FP32 gate at **0.911%** output relative L2; NPU-versus-QDQ-CPU error was **0.0541%**. Native Windows 11 build 26200, NPU driver `32.0.203.329`, ONNX Runtime `1.30.0` and VitisAI EP `1.8.63.0` are fixed in the [environment record](environment.json). Its nearest-rank 20-call p50/p95, including the first inference after session creation, was **234.95/250.78 ms** with VitisAI versus **567.05/576.19 ms** for the same QDQ graph on ORT CPU. Session creation took **14.92 s** on the NPU and is excluded from those call timings. These are isolated, same-process component calls; Omni dispatch, downstream execution, transfer, shared-RAM admission and power are excluded. The [raw first candidate trace](full4_npu_profile.json.gz), [raw selective trace](first3_npu_profile.json.gz) and [selective worker log](first3_worker.log) are retained. Each gzip file decompresses to the SHA-256 in its corresponding probe report.

An independent [blue-circle/green-triangle image](heldout_blue_circle.png) then supplied a held-out input at the same fixed shape. Its [FP32 export and BF16 reference](heldout_export_report.json) reproduced the exact four-layer ONNX graph hash without recalibration. Reusing the selective candidate gave **6.864% relative L2 versus FP32** and **6.929% versus BF16** in the [held-out NPU report](heldout_probe.json), decisively failing the 1% gate. NPU remained within **0.0492%** of the same QDQ graph on CPU and still placed 15 VitisAI nodes per call, so the evidence points to the single-image quantization rather than NPU execution. The [raw held-out placement trace](heldout_npu_profile.json.gz) and [worker log](heldout_worker.log) are retained. The calibration-image pass must not be generalized to a vision component pass.

Recalibrating that same three-layer cut on both red-square and blue-circle activations produced a new pinned A16W8 candidate (SHA-256 `5e3d0ed188db01c643628411725614fd82927bed2a76f93a370085f49c5f2624`). It passed the provisional FP32 boundary gate on both calibration inputs: [red-square NPU](twoimage_primary_probe.json) **0.911%** and [blue-circle NPU](twoimage_blue_probe.json) **0.926%** relative L2. A third, unseen [gradient-grid image](heldout_gradient_grid.png) gave a [held-out NPU result](twoimage_third_probe.json) of **1.579%** versus FP32 and **1.878%** versus BF16, failing the gate. All three evaluations placed 15 VitisAI nodes per call over 20 calls; on the third image, NPU differed only **0.0400%** from QDQ CPU. The [raw red](twoimage_primary_profile.json.gz), [blue](twoimage_blue_profile.json.gz) and [third-image](twoimage_third_profile.json.gz) traces, corresponding [third-image export](third_export_report.json), and [worker logs](twoimage_third_worker.log) are retained. This two-image calibration improves its included images but still does not generalize to an independent image.

To test the full vision transformer handoff, the [same pinned weights](layer3_red_export.json) were also exported at the earlier **three-layer** boundary (182,897,665-byte ONNX, SHA-256 `b68fe07f9ae74d2c4d3a953b43ce5b33e28f86a41f413f857d2e00edf67b9efb`). A two-image A16W8 candidate (SHA-256 `bba44f82fff3964ff8c3d2470e45f2fb529bc4e049a098a0c9bfef75c0fae110`) put 15 VitisAI nodes per call on the HX370 NPU, leaving **layers 4–27 and post-layernorm in unchanged BF16 Torch on CPU**. The [red 20-call probe](layer3_red_probe.json) measured nearest-rank p50/p95 **156.81/176.67 ms** on NPU versus **467.00/475.76 ms** for its QDQ CPU graph, after a **13.65 s** NPU session creation. The [blue](layer3_blue_probe.json) and [third-image](layer3_third_probe.json) captures are one NPU call each, not latency profiles. Their [raw red](layer3_red_profile.json.gz), [blue](layer3_blue_profile.json.gz) and [third](layer3_third_profile.json.gz) placement traces are retained.

The measured NPU tensors are retained as [red](layer3_red_output.npz), [blue](layer3_blue_output.npz) and [third](layer3_third_output.npz) boundary artifacts. Replaying each through the exact BF16 CPU suffix with the [handoff audit](../../../../experiments/audit_minicpmo_vision_handoff.py) produced [red](layer3_handoff_red.json), [blue](layer3_handoff_blue.json) and [third](layer3_handoff_third.json) post-layernorm errors of **9.276%**, **7.660%** and **9.077%** relative L2 versus the same checkpoint's all-BF16 vision path. The unquantized FP32 boundary sent through that same BF16 suffix already differed **8.576%**, **6.672%** and **7.576%**. This control shows that converting the early checkpoint layers to FP32 is a major source of the downstream drift; the NPU candidate adds further error. The prior four-layer NPU capture with BF16 layers 5–27 had [red](handoff_red.json), [blue](handoff_blue.json) and [third](handoff_third.json) post-layernorm errors of **9.183%**, **7.747%** and **9.096%**. Moving the cut back one layer did not fix quality. These are real NPU outputs and a full SigLIP transformer/post-layernorm replay, but still exclude image resampling, thinker output, complete multimodal behavior, transfer-inclusive latency and power.

A separate [BF16-preserving one-layer export](bf16_layer1_export.json), reproduced by the [BF16 probe](../../../../experiments/probe_minicpmo_vision_bf16_npu.py), tested precision alignment before A16W8 calibration. The direct 30,486,919-byte ONNX graph (SHA-256 `58919d088a7f1516d657ea8f0c03e8101325deba6b7ab0417c4ee25c51627348`) retained BF16 input, output and internal weights. WSL ORT 1.29 [could not create a CPU session](bf16_layer1_ort_cpu_report.json) because its BF16 MatMul kernel is absent. Native Windows VitisAI 1.8.63.0 did create a [direct graph session](bf16_layer1_direct_report.json) after **321.94 s**, but the ORT 1.30 Python binding rejected its BF16 NumPy input before inference. Adding only FP32 input/output casts produced a 30,487,066-byte graph (SHA-256 `6fe2bfa6ce6af5857e88066534b006935b3716f7206c278c35d0460ae58e7ae5`) with BF16 computation inside. On HX370 driver 32.0.203.329, its [native report](bf16_layer1_iofp32_report.json) and [raw profile](bf16_layer1_iofp32_profile.json.gz) show **one VitisAI node and no CPU node events**. Session creation took **325.21 s**; the one 35.72 ms call is not a repeated latency profile.

The exact [Torch BF16 reference](bf16_layer1_reference.npz) and [measured NPU output](bf16_layer1_iofp32_output.npz) have [1.182% relative L2 error](bf16_layer1_audit.json) at layer one, missing the provisional 1% component gate. Replaying that output through unchanged BF16 Torch layers 2–27 and post-layernorm gave [13.863% raw / 21.035% post-norm relative L2](bf16_layer1_suffix_audit.json) versus the matching Torch BF16 path from the same fixed input activation. The [native worker log](bf16_layer1_iofp32_worker.log) and [direct-input failure log](bf16_layer1_direct_worker.log) are retained. This is one synthetic image activation, one NPU call and an offline CPU suffix replay; no complete MiniCPM-o image request, task-quality tolerance, transfer-inclusive timing or power result follows. The BF16 graph is **not numerically qualified** by this experiment.

The same BF16 graph then ran red, blue and independent gradient inputs in **one native session**. The [three-image report](bf16_layer1_multi_report.json), [exact outputs](bf16_layer1_multi_output.npz) and [raw profile](bf16_layer1_multi_profile.json.gz) show three VitisAI node events and no CPU node events after **321.30 s** session creation. One call per input took 31.78/26.62/26.80 ms; these are not repeated latency samples. Relative L2 against the corresponding Torch BF16 layer was **1.182%/1.249%/1.270%**, so all three miss the 1% gate. The [blue](bf16_layer1_blue_export.json) and [third](bf16_layer1_third_export.json) reference exports have the same ONNX graph hash; their exact [blue](bf16_layer1_blue_reference.npz) and [third](bf16_layer1_third_reference.npz) Torch tensors are retained. Replaying the measured NPU outputs through unchanged BF16 layers 2–27 and post-layernorm gave **21.035%/16.875%/9.136%** relative L2 on [red](bf16_layer1_multi_red_suffix_audit.json), [blue](bf16_layer1_multi_blue_suffix_audit.json) and [third](bf16_layer1_multi_third_suffix_audit.json). The [worker log](bf16_layer1_multi_worker.log) is retained. This confirms placement across the three inputs but does not qualify the one-layer split or a complete image request.

An independent RTX 5090 Laptop Torch BF16 control rules out a CPU-only reference effect. CPU versus CUDA BF16 layer-one outputs differ 0.217%/0.210%/0.262% relative L2 on red/blue/third, while the same NPU outputs differ [1.180%](bf16_layer1_cuda_red_suffix_audit.json)/[1.251%](bf16_layer1_cuda_blue_suffix_audit.json)/[1.272%](bf16_layer1_cuda_third_suffix_audit.json) from CUDA BF16. The unchanged CUDA BF16 suffix yields 21.018%/16.761%/9.138% post-layernorm error, close to the CPU suffix results above. These CUDA controls used Torch 2.13.0+cu130, RTX 5090 Laptop driver 610.71 and TF32 disabled; they are numerical controls, not comparable latency measurements. A separate [DirectML adapter-1 attempt](bf16_layer1_dml_report.json) on ORT 1.24.4 failed session creation at BF16 MatMul, so it produced no numerical control or placement trace; its [worker log](bf16_layer1_dml_worker.log) is retained.

To bisect the first-layer error, an [instrumented BF16 export](bf16_attn_export.json) exposes the attention residual and full layer output while preserving the same fixed inputs. The [real HX370 NPU run](bf16_attn_npu_report.json) placed one VitisAI node per image in its [raw profile](bf16_attn_profile.json.gz), after 325.41 s session creation. Its [captured outputs](bf16_attn_npu_output.npz) match the earlier one-output NPU full-layer tensors **bitwise** on all three images, so observation did not change that result. Against the [Torch BF16 intermediates](bf16_attn_reference.npz), attention-residual errors are **0.709%/0.821%/0.846%**, inside the provisional 1% component gate. The [hybrid replay](bf16_attn_hybrid_audit.json) runs the exact BF16 Torch MLP on those measured NPU attention tensors and reaches **0.625%/0.682%/0.813%** full-layer error, versus **1.182%/1.249%/1.270%** when the NPU also runs the MLP. Yet its unchanged BF16 CPU vision suffix still has **11.034%/9.003%/6.819%** post-layernorm error. The [bisect probe](../../../../experiments/bisect_minicpmo_bf16_vision_layer.py) and [native log](bf16_attn_npu_worker.log) are retained. This attention-only route is a component numerical candidate on three synthetic inputs, with no task-quality gate, complete request, transfer-inclusive speedup, memory admission or power evidence.

The source BF16 comparison remains a quality blocker: the selective NPU candidate differs **1.425% relative L2** from the checkpoint's BF16 four-layer output on the calibration image, without a downstream task tolerance. A separate [27-layer transformer-block FP32 export](full_export_report.json) is numerically faithful to FP32 Torch at **9.24e-6 relative L2** but differs **5.311%** from BF16 Torch on that image before quantization. Its 1,646,081,657-byte ONNX artifact remains outside Git at `/home/zhout/project/edge_infer/models/minicpmo_vision_prefix27_448_20260925.onnx`, SHA-256 `534641cb3589df399b90f78c4fb3629079f824224eb0bd65801d26725cb16134`. This graph excludes post-layernorm and resampler and was **not run on NPU**.

A [last-layer BF16 attention attempt](late27_export.json) moved the same two-output boundary to SigLIP layer 27 (index 26). The unchanged BF16 Torch prefix generated three `[1,1024,1152]` layer inputs from the pinned red, blue and gradient fixtures, with [exact inputs](late27_layer_inputs.npz) and [Torch attention/full references](late27_reference.npz) retained. The 30,485,895-byte wrapped ONNX graph (SHA-256 `37c17644840d9ec9f8154d762b28e014b6f4aa524c48b9b0e3759309e2633f3b`) remains outside Git at `/home/zhout/project/edge_infer/models/minicpmo_vision_late27_20260925/wrapped.onnx`. On native Windows HX370, [one session](late27_npu_report.json) created in **317.65 s** and executed one VitisAI node for each of the three inputs, with no CPU node events. Its three one-off calls took 32.07/28.74/27.87 ms; they are not a repeated or transfer-inclusive profile. The [raw provider trace](late27_npu_profile.json.gz) decompresses to the report's SHA-256, and [measured tensors](late27_npu_output.npz) are retained.

The [same-checkpoint BF16 replay](late27_audit.json) puts the measured NPU **attention** residual through the unchanged CPU BF16 MLP, post-layernorm and resampler. Relative L2 versus the all-BF16 path is **0.867%/0.925%/0.903% after post-layernorm**, but **1.477%/1.556%/1.462% after the `[1,64,4096]` resampler** on red/blue/gradient. Letting NPU execute the full layer instead gives **1.551%/1.663%/1.494%** after the same resampler. Thus moving NPU work from layer one to layer 27 repairs the provisional post-norm tensor gate on these three synthetic fixtures, but the complete vision embedding still misses the 1% numerical gate. This is not an image-answer or multimodal request, and no quality tolerance, transfer-inclusive benefit, admission, cancellation or power result exists. The checkpoint shard, fixture, graph, activation, output and raw-profile hashes in the three reports constrain the conclusion to this exact artifact and shape.

A further [FP32 layer-27 export](late27_fp32_export.json) promoted the same BF16 checkpoint weights to FP32 only for that last layer, reusing the pinned BF16 prefix activations. The 30 MB ONNX graph remains outside Git at `/home/zhout/project/edge_infer/models/minicpmo_vision_late27_fp32_20260925/wrapped.onnx` (SHA-256 `54dae2b12357ff730775bc419d9ecfb90069455a3ca22c73cf60ef29b3d7b203`). ONNX CPU matched FP32 Torch within 2.61e-7 relative L2 on both outputs for all three images. The FP32 **CPU** last-layer control, cast back to BF16 before unchanged post-layernorm and resampler, reached 0.912%/0.832%/0.883% embedding relative L2 against all-BF16 on red/blue/gradient. That control alone passes the provisional 1% embedding gate, but it is not an NPU result.

The [native HX370 FP32 NPU run](late27_fp32_npu_report.json) used that exact graph and the same activations. Its [raw ORT trace](late27_fp32_npu_profile.json) records one VitisAI fused node per image and no CPU node event; session creation took **311.59 s**. The three one-off calls took 32.34/28.37/27.56 ms, without a repeated or transfer-inclusive profile. Its [measured tensors](late27_fp32_npu_output.npz) differ from FP32 Torch by 0.407%/0.443%/0.489% at the attention residual and 0.668%/0.699%/0.765% at the full layer. Replaying those measured **full-layer** tensors through unchanged BF16 post-layernorm and resampler gives **1.549%/1.551%/1.519%** embedding relative L2 versus all-BF16. Replaying NPU attention through the BF16 CPU MLP gives **1.526%/1.484%/1.512%**. Both miss the 1% gate. The native report's `scope` string mistakenly says BF16; its graph hash matches the explicitly FP32 [export report](late27_fp32_export.json), which also pins the FP32 [Torch reference](late27_fp32_reference.npz). The probe source now records the export precision for future runs; the original native report is retained unchanged.

The [offline correction audit](late27_affine_correction.json) fits a per-channel affine map on red and blue NPU attention outputs and evaluates the independent gradient. It leaves the gradient embedding at **1.413%** relative L2 for FP32 NPU and **1.449%** for BF16 NPU. These maps do not qualify either cut and were not inserted into a live execution path. As a coarser candidate, a complete **FP32 CPU resampler** receiving the unchanged BF16 SigLIP output differs **1.082%/0.930%/0.878%** from the BF16 resampler on red/blue/gradient. It already misses the provisional gate on red before any NPU execution, so this exact FP32 resampler artifact was not compiled or timed on NPU. A BF16-preserving resampler or an empirically justified image-task tolerance remains possible, but unverified.

The [BF16-preserving whole-resampler experiment](resampler_bf16_export.json) fixes batch one and the 32×32 patch bucket. Its [export wrapper](../../../../experiments/probe_minicpmo_resampler_bf16_npu.py) removes only the source's unnecessary dynamic `pad_sequence` for this shape and reproduced the original Torch BF16 resampler output **bitwise** on red/blue/gradient before export. The wrapped ONNX graph has FP32 boundary casts and BF16 internal weights/computation; its 186,212,517-byte graph remains outside Git at `/home/zhout/project/edge_infer/models/minicpmo_resampler_bf16_20260925/wrapped.onnx`, SHA-256 `7c0ba0e1aa07a167be0daf2b61602a181e456ec5954a9aa58acceccf7f6b3977`. The [normalized SigLIP inputs](resampler_bf16_inputs.npz) and [BF16 Torch embeddings](resampler_bf16_reference.npz) are retained.

On native Windows 11 build 26200, ORT 1.30.0 and VitisAI EP 1.8.63.0 placed **one fused NPU node per image** in the [raw provider trace](resampler_bf16_npu_profile.json), with no CPU node event. The [native report](resampler_bf16_npu_report.json) records an **85.30 s session build** and one-off 50.22/48.53/46.03 ms calls. The [measured outputs](resampler_bf16_npu_output.npz) differ **3.516%/3.114%/3.101% relative L2** from the exact BF16 Torch embedding on red/blue/gradient. All are finite but fail the provisional 1% vision-embedding gate. The [native worker log](resampler_bf16_worker.log) is retained. These are component C placement and failure measurements, without a repeated latency profile, transfer/admission cost or complete image request; the BF16 resampler split is not enabled.

An exact-graph [ORT 1.30 CPU control](resampler_bf16_cpu_report.json) could not create a session because its CPU EP lacks the BF16 `MatMul(13)` kernel at `/kv_proj/MatMul`. The fixed-shape **Torch wrapper** matches the source bitwise, but there is no executable ORT CPU graph output to separate ONNX-export behavior from VitisAI execution error. The failure is therefore assigned to this tested graph/runtime path, not to BF16 resampling on all devices.

Next, separate ONNX-export and VitisAI numerical drift at the resampler operator boundary, or establish a downstream image-task tolerance on representative independent images. None of the tested first-layer, last-layer, FP32-resampler or BF16-resampler candidates passes a full vision-stage gate. Only after such a gate should a complete MiniCPM-o request test transfer-inclusive latency, shared-RAM admission, cancellation and sustained power. The earlier [NPU speech-token projection](../minicpmo_amd_npu_projection/README.md) remains a separate one-activation component; its per-token split was slower than the FP32 CPU equivalent and is not enabled by default.

Reproduce the export with `--layers 3`, `--layers 4` or `--layers 27` using the pinned paths above, then copy its ONNX and fixture to native Windows storage. Run the candidate probe with `--source`, `--fixture`, `--candidate`, `--report`, `--ep-dir`, `--profile-prefix`, `--layers 3` or `--layers 4`, and `--samples 20`; use `--exclude-last-layer` only for the four-layer selective candidate. For a second calibration input, supply `--extra-calibration-fixture` and its SHA-256 when creating a new candidate. For an independent evaluation, pass `--fixture-sha256` and `--candidate-sha256` to reuse the artifact without recalibration. Add `--output-npz` to retain the exact NPU and QDQ CPU boundary tensors, then run the handoff audit with `--model-dir`, `--fixture`, `--npu-report`, `--npu-output`, `--boundary-layer 3` or `4`, and `--report`. The exact fixture and artifact hashes are in the reports; the probe and audit refuse changed inputs, and the probe retains its raw provider profile. The large ONNX source/candidate files and model weights remain outside Git; the measured boundary tensors, reports, and profiles are retained here.

For the BF16 route, run the BF16 probe's `export` subcommand with the pinned model directory, a three-layer fixture, direct/wrapped ONNX destinations, reference NPZ and report; pass `--fixture-sha256` for blue or third. Copy the wrapped graph, fixtures and references to native Windows storage; run its `run-multi` subcommand with the Windows VitisAI `ExecutionProvider` directory, a profile prefix, output NPZ and report. Its `audit` subcommand takes the export report, NPU report, reference, measured multi-output NPZ and case name to replay the BF16 CPU suffix. The exact ONNX graph hashes are above; neither 30 MB graph is checked into Git.

For the attention split, use the bisect probe's `export` subcommand on the three pinned fixtures to produce its two-output BF16 graph, wrapped FP32-I/O graph, Torch reference and report. Copy the wrapped graph, reference and fixtures to native Windows, then use `run` with the VitisAI EP directory and profile prefix. Finally, run `audit` with the measured output, the preceding one-output NPU capture, checkpoint, export and run reports. The wrapped graph is 30,485,895 bytes with SHA-256 `f043439f0480cffcda99ec35b1b6316565d220aa52fa376e219db05225fe2b65`; it remains outside Git. The retained NPZs, raw placement profile and report hashes allow exact audit of the stated boundary results.

For the layer-27 cut, add `--layer-index 26 --layer-inputs <path>` to `export`; pass the saved layer inputs to `run` with `--layer-inputs`, and run `audit --resampler` without `--prior-output` against the retained [reference](late27_reference.npz), [NPU output](late27_npu_output.npz), [export report](late27_export.json) and [native report](late27_npu_report.json). The probe validates all input and output hashes and the NPU provider count before replaying the unchanged checkpoint MLP, post-layernorm and resampler. The WSL export/audit used PyTorch 2.13.0+cpu with eight CPU threads; native NPU execution used Windows 11 build 26200, driver 32.0.203.329, ORT 1.30.0 and VitisAI EP 1.8.63.0. Power and concurrent load were uncontrolled.

To reproduce the FP32 last-layer control, rerun that export with `--compute-dtype fp32 --reuse-layer-inputs-sha256 61a28d3cae38413fe2feb7161118fe7421f1a452bc2fa2b523c34a65007c339d` and separate FP32 graph/reference/report destinations. Run its `run` subcommand on native Windows with that graph, reference and unchanged layer inputs. Then run [the correction/replay probe](../../../../experiments/probe_minicpmo_late27_correction.py) with the original BF16 `--reference`, `--npu-output`, `--layer-inputs`, pinned `--model-dir`, and all four `--fp32-*` report/reference/output options. It verifies the checkpoint, graph, reference and captured-output hashes before comparing the BF16 resampler embeddings. The [native worker log](late27_fp32_worker.log) is retained with the output and trace; its model compile time is included only in the 311.59 s session creation, not the one-off call timings.

For the whole-resampler probe, run its `export` command with the pinned model directory and `late27_reference.npz`, then copy the wrapped graph, input/reference NPZs, export report and probe script to native Windows storage. Run its `run` command with the exact graph and `ExecutionProvider` directory; it validates their hashes before session creation and retains the NPU output, raw provider profile and report. The graph is fixed to `[1,1024,1152] → [1,64,4096]` at 32×32 patches. The native one-off calls do not include the 85.30 s load or any Omni transfer.
